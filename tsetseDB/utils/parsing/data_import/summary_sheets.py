# summary_sheets.py is part of the 'tsetseDB' package.
# It was written by Gus Dunn and was created on 8/25/14.
# 
# Please see the license info in the root folder of this package.

"""
=================================================
summary_sheets.py
=================================================
Purpose:

"""
__author__ = 'Gus Dunn'


def validate_summary_data_format(worksheet):
    """
    Returns `True` if `worksheet` passes format tests, `False` otherwise.
    :param worksheet:
    :return: `bool`
    """


def import_district(worksheet_row):
    """
    Adds district name to correct table if new.

    :param worksheet_row:
    :return:
    """


def import_county(worksheet_row):
    """
    Adds county name to correct table if new.

    :param worksheet_row:
    :return:
    """


def import_subcounty(worksheet_row):
    """
    Adds district name to correct table if new.

    :param worksheet_row:
    :return:
    """


def import_parrish(worksheet_row):
    """
    Adds district name to correct table if new.

    :param worksheet_row:
    :return:
    """


def import_village(worksheet_row):
    """
    Adds village name to correct table if new.

    :param worksheet_row:
    :return:
    """


def import_trap_no(worksheet_row):
    """
    Adds trap number to correct table if new.

    :param worksheet_row:
    :return:
    """


def import_trap_latlong(worksheet_row):
    """
    Adds trap's latitude and longitude to correct table if new.

    :param worksheet_row:
    :return:
    """


def import_trap_elevation(worksheet_row):
    """
    Adds trap elevation to correct table if new.

    :param worksheet_row:
    :return:
    """


def import_trap_human_activity(worksheet_row):
    """
    Adds human activity near trap to correct table if new.

    :param worksheet_row:
    :return:
    """


def import_trap_vegetation(worksheet_row):
    """
    Adds vegetation-type near trap to correct table if new.

    :param worksheet_row:
    :return:
    """


def import_trap_deployed_on(worksheet_row):
    """
    Adds trap's deploy date to correct table if new.

    :param worksheet_row:
    :return:
    """


def import_trap_removed_on(worksheet_row):
    """
    Adds trap's removal date to correct table if new.

    :param worksheet_row:
    :return:
    """


def import_other_info(worksheet_row):
    """
    Adds "other info" column to correct table if new.

    :param worksheet_row:
    :return:
    """
